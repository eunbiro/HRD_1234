function fn_submit() {
	var fn = document.frm;
	
	if (fn.custname.value == "") {			// input값이 빈값인지 체크
		alert("회원성명이 입력되지 않았습니다.");
		fn.custname.focus();				// 포커싱주기
		return false;						// 함수 끝내기
	}
}